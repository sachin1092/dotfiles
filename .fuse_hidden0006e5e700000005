#! /bin/sh
while read line
do
	echo "$line"
    HOST=`echo $ENTRY | cut -d/ -f3`
    USER=`echo $ENTRY | cut -d/ -f4`
    PPA=`echo $ENTRY | cut -d/ -f5`
    echo "HOST - $HOST"
    echo "USER - $USER"
    echo "PPA - $PPA"
done < installedapps